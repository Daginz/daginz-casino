'use client';

import * as s from './shell.css';

export function Footer() {
  return (
    <footer className={s.footer}>
      <div>Daginz · testnet demo · no real-money gambling.</div>
      <div>Valueless test chips only. Provably fair via commit-reveal.</div>
    </footer>
  );
}
