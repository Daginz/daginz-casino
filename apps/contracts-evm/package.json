{
  "name": "@casino/contracts-evm",
  "version": "0.0.0",
  "private": true,
  "description": "Casino EVM contracts: CHIP ERC-20 + faucet + escrow (Hardhat)",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "smoke:onchain": "hardhat run scripts/onchain-smoke.ts --network localhost",
    "smoke:withdraw": "hardhat run scripts/withdraw-check.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
